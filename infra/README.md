# Infrastructure Skeleton

This folder is reserved for infrastructure-as-code and deployment descriptors.

Suggested next steps:
- Add Terraform modules for network, compute, storage, and secrets.
- Add Kubernetes manifests or Helm charts for backend, workers, and frontend.
- Add Prometheus/Grafana stack manifests.
